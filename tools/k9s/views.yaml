# Custom columns. NOTE: a columns definition REPLACES the default set entirely.
views:
  v1/pods:
    columns:
      - NAME
      - READY
      - STATUS
      - RESTARTS
      - CPU
      - MEM
      - IP
      - NODE
      - QOS
      - AGE
